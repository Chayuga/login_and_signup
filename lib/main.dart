import 'package:chuify/screens/login.dart';
import 'package:flutter/material.dart';

//void main() => runApp(Login());

void main() => runApp(
      new MaterialApp(
        home: Login(),
      ),
    );
